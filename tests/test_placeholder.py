def test_placeholder():
    """
    Teste inicial para garantir que o pytest não falhe com exit code 5 (no tests collected).
    Pode ser removido assim que testes reais forem adicionados.
    """
    assert True
